const {db,init}=require('./_db');module.exports=async(req,res)=>{await init();let id=String(req.query.id||'');let q=await db.execute({sql:'SELECT * FROM requests WHERE id=? AND status=\'approved\'',args:[id]});let r=q.rows[0];if(!r||Number(r.expires_at)>Date.now())return res.status(403).send('Download is not available yet.');if(!process.env.BOT_FILE_URL)return res.status(503).send('Admin has not configured the bot file yet.');res.redirect(process.env.BOT_FILE_URL)};